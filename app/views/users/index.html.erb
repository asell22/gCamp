<% if notice %>
  <div class= "alert alert-success">
    <%= notice %>
  </div>
<% end %>



<div class="page-header">
  <div class = "pull-right">
    <%= link_to "New User", new_user_path, class: "btn btn-info" %></button>
  </div>
  <h1>Users</h1>
</div>


<table class = "table table-strip">
  <thead>
    <tr>
      <th>Name</th>
      <!-- <th colspan="3"></th> -->
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
    <tr>
      <td><%= user.first_name %> <%= user.last_name %></td>
      <td><%= mail_to user.email %></td>
      <td class="pull-right">
        <%= link_to 'Edit', edit_user_path(user), class: "btn btn-info" %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
